'''
sequenceDiagram
    participant browser
    participant server

    Note right of browser: The browser prevents the default form submit, adds the note to the local list, and re-renders the note list on screen

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    
    server-->>browser: HTTP 201 Created ({"message":"note created"})
    deactivate server
  '''